-# .content
-#   .content__left
-#     .content__left--image
-#   .content__right
-#     .content__right__top
-#       = link_to post.image, post_path(post.id), class: "content__right__top--title"
-#     .content__right__bottom
-#       .content__right__bottom--userName
-#         = link_to post.user.name, user_path(post.user), class: "content__right__bottom--userName--btn"
-#       .content__right__bottom--date
-#         = post.created_at

.content
  .content__left
    .content__left--image
      = link_to post.image, post_path(post.id), class: "content__right__top--title"
    .content__left--coupon
      = link_to post.couponcode, post_path(post.id), class: "content__right__top--title"
  .content__right
    .content__right__name
      = link_to post.shopname, post_path(post.id), class: "content__right__top--title"
    .content__right__address
      = link_to post.address, post_path(post.id), class: "content__right__top--title"
      -# = link_to post.user.name, user_path(post.user), class: "content__right__bottom--userName--btn"(住所挿入)
    .content__right__date
      = link_to post.expirydate, post_path(post.id), class: "content__right__top--title"
      -# = post.created_at(有効期限挿入)
