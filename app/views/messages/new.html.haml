= render partial: "posts/postpost"

.contents
  .content__title
    注文内容

  .content__all
  .content__whole
    -# = form_with(url: posts_path, local: true, method: :get, class: "form") do |form|
    = form_with model: @message, class: :form,url: :user_messages, local: true do |form|
      .field
        = form.label :クーポンコードを記入してください（必須）
        = form.text_field :couponcode, class: :form__text, id: :couponcode

      .field
        = form.label :名前を記入してください（必須）
        = form.text_field :name, class: :form__text, id: :name

      .field
        = form.label :電話番号を記入しください（必須）
        = form.text_field :phonenumber,class: :form__text, id: :phonenumber

      .field
        = form.label :受け取り希望の時間を記入してください（必須）
      .field_select
        = form.select :ordertimeleft, ['0時','1時', '2時','3時','4時','5時','6時','7時','8時','9時','10時','11時','12時','13時','14時', '15時','16時','17時','18時','19時','20時','21時','22時','23時']
        = form.select :ordertimeright, ['00分', '05分','10分','15分','20分','25分','30分','35分','40分','45分','50分','55分']

      .field
        = form.label :ご利用方法を選択しください（必須）
        = form.select :howtoeat, ['お持ち帰り', '店内'], {include_blank: '選択'}, class: 'sample'

      .field
        = form.label :品数を記入してください（必須）
        = form.select :people, ['1個', '2個','3個','4個','5個','6個','7個','8個','9個','10個'], {include_blank: '選択'}, class: 'sample'

      .field
        = form.submit '注文する', class: :form__btn